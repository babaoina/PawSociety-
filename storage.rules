rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Default rule - allow all for emulator testing
    match /{allPaths=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
