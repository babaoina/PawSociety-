rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // EMULATOR-ONLY MODE: Allow all operations for testing
    // These rules are PERMISSIVE - do not deploy to production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
